# Meta-Agent Mapping Resume — Pro Startup Prompt

> Use only after the mechanical inventory PR is merged with `PASS_TO_FRONTIER_MAPPING_RESUME`.

```text
@GitHub 请读取并严格执行：

handoff/meta-agent-dedicated-repository-mapping-resume-taskbook.md

执行任务：
META-AGENT-DEDICATED-REPOSITORY-MAPPING-RESUME-001

当前对话唯一主线保持为 META_AGENT_PRODUCT_BUILD，并使用 GPT Pro/frontier 级推理。

开始前必须核验已合并的机械 inventory：
META-AGENT-DEDICATED-REPOSITORY-MECHANICAL-INVENTORY-001

只验证其 source commit、root subtree SHA、closure、manifest hash 和源树未变化。若这些证据有效，不得重新运行完整递归枚举、不得重复工具能力探索、不得重复 receive-only 测试。

在 08822407d/Mnemosyne 中完成：
- PR #255 post-merge closeout 与 live navigation 修复；
- 每个 E0 blob 的最终 authority / memory / material / migration 语义分类；
- 两套完整 destination mapping；
- history strategy 比较；
- Meta-Agent-owned behavior guidance candidate 与 adoption matrix；
- 初始 memory-system alignment；
- Owner initialization decision package；
- 一个任务分支和至多一个 PR。

严格禁止：
- 写入或初始化 08822407d/Meta-Agent；
- 修改 approved-spec、authority map、accepted methodology、case ledger 或 migration log；
- 自动采用 behavior guidance 或 memory design；
- shadow copy、cutover、private material、prototype、pilot 或 activation。

如果 E0 后 target-projects/meta-agent/ 发生变化，返回：
BLOCKED_SOURCE_TREE_CHANGED_AFTER_E0
并只请求机械 delta refresh，不要把旧 manifest 用于新源树。

完成一个 Mnemosyne PR 后停止，等待 Owner 审查。
```
