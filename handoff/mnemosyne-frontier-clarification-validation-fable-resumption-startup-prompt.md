# Mnemosyne Frontier-Clarification Validation Fable Resumption — Startup Prompt

> Future use only after an explicit user decision to open a separate dedicated conversation. First round is receive-only and preserves the indefinite pause.

```text
@GitHub 请读取并严格执行：

handoff/mnemosyne-frontier-clarification-validation-fable-resumption-package.md

第一轮只接收并核验暂停状态，返回：

fable_route_resumption_receive

然后停止。

不要启动 Fable、Research、Deep Research、A1、A2、V0、V1、V2 或 V3；
不要创建 Project、启用 connector、消耗 quota、写仓库、修改验证包，
也不要导入 Meta-Agent、non-FABLE health-review、Adaptive Explanation
或其他 Mnemosyne 路线。

必须从执行时最新 08822407d/Mnemosyne master 读取 handoff 指定的文件，
确认：

- Fable 路线仍为 Owner 指示的无限期暂停；
- A1 没有有效 substantive report；
- A2 仍依赖有效 A1 裁决；
- 已完成的付费 Project-knowledge probe 不得原样重跑；
- 未来任何 paid run 仍需当前产品表面复核、显式 RUN disposition 和 quota 授权。

第一轮返回 receive object 后等待我的下一条命令。
```
