# 实测任务书 · MNE-DR-027 ChatGPT GitHub 写能力实测（普通对话，非深度研究）

> 单粘贴任务书。请完整读取后按步骤执行；这是一次**有边界的能力实测**，不是开发任务。

```yaml
display_name: MNE-DR-027 GitHub写能力实测
canonical_task_id: FABLE5-REDESIGN-001-RQ12
execute_in: ChatGPT 官方入口 · 新普通对话（非深度研究）· 模型/智能程度选 "Pro" · 需已连接 GitHub（08822407d/Mnemosyne）
expected_duration: 约 10 分钟
```

## 1. 背景（给执行对话看）

2026-07 本仓库实测过普通 ChatGPT 对话经 GitHub 连接执行写入（建分支/文件/PR，如当时的 PR #133）；但 2026-08-31 核查发现 OpenAI 当前官方文档称标准 GitHub 连接为**只读**、写入指向 Codex。两者矛盾，需要一次现场实测定分晓。结论将决定后续工作流里"ChatGPT 侧能否直接写仓库"。

## 2. 执行步骤（逐步做并记录每步结果）

1. 确认当前对话可读仓库：读取 `08822407d/Mnemosyne` 默认分支的 `README.md` 首行并引用。
2. 尝试**创建分支** `chatgpt-write-capability-test-20260831`（自默认分支）。记录：是否出现该操作的选项/审批卡；批准后是否成功；任何错误原文。
3. 若步骤 2 成功：尝试在该分支**新建文件** `notes/platform-checks/chatgpt-github-write-test-20260831.md`，内容为本次各步结果的简要记录（含时间、模型选择器显示值）。
4. 若步骤 3 成功：尝试**创建 PR**（标题 `MNE-DR-027: ChatGPT write capability test — DO NOT MERGE`，草稿或普通均可）。**不要合并**。
5. 汇总输出一张表：步骤 / 试图使用的 action / 审批卡出现与否 / 结果（成功、失败原文、无此能力）。若任一步无对应能力，如实写"当前界面无此操作"并停止后续写入步骤。

## 3. 边界（必须遵守）

- 只允许上述一个分支、一个文件、一个 PR；**禁止合并、删除、修改其他任何内容或其他仓库**。
- 无写入能力就报告无能力——不要绕道（不要用 Codex、不要建议其他方案，此次只测本表面）。
- 结果表直接写在对话回复里（操作者会复制带回）；无需生成文件。

## 4. 操作者收尾（给 Owner）

- 把结果表（连同你看到的审批卡行为描述）复制发回 Claude 侧会话；
- 若 PR 已创建：不合并，保留或关闭均可（Claude 侧会记录后续处置）。
