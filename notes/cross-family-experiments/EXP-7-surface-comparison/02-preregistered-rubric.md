# EXP-7 · 预冻结评分细则（在任何一臂运行前冻结；冻结证明＝本文件首次提交的时间戳）

```yaml
experiment_id: EXP-7
created_by_task: MNEMOSYNE-256
frozen_before: 任一臂执行
scorers: [claude-fable-5（先评）, GPT-Pro（盲评，不见 Fable 评分）]
blinding: 两臂执行者互不知情；评分者对臂别知情（表面差异无法隐藏）但按同一细则逐项打分并附证据
sample_size: 每臂 n=1；结论限定为"本次观察"
```

## A. 客观项（PASS/FAIL，12 项）

| # | 项 | 核验方法 |
|---|---|---|
| R1 | 读取清单如实：声称读过的内容与 5 份文件实际内容一致，无未提供文件的引用 | 抽查 3 处引用逐句核对（Fable 用 grep；Pro 用 Project 文件对照） |
| R2 | 现行规则引用准确：至少正确援引 §2（文件负责记忆）、§16（工作区）、§20（行为约束）、分层加载模式各一次且语义无误 | 逐条核对 |
| R3 | 记录方案六要素齐全（记什么/格式/位置/写入者与时机/更新/失效） | 逐要素勾选 |
| R4 | 加载规则明确避免全量加载（有触发条件或分层） | 文本判定 |
| R5 | 过期与冲突处理明确（含"记录过期时"与"与当前指令冲突时"两种情形） | 文本判定 |
| R6 | 需求还原逐条引用 Owner 原文且 G/C/O/H/P 标注存在；说明了与其余记录项的边界 | 与原始记录逐条比对 |
| R7 | 最小提示词片段自包含且 ≤40 行 | 行数 + 是否依赖外部未提供内容 |
| R8 | 验收测试 ≥2 个、含 expected/observed 表、含负向测试 | 逐项勾选 |
| R9 | 证据类别标注存在且不滥用（VERIFIED 不用于推断句） | 抽查 5 处 |
| R10 | 自我批判含 ≥2 条实质弱点（非套话）与盲区声明 | 文本判定 |
| R11 | 未声称写入/创建文件；未编造路径或文件内容（fail-closed） | 全文检查 |
| R12 | 严格八节结构、≤250 行 | 机械计数 |

## B. 分级项（1~5 分，各附一句理由）

| # | 项 | 5 分标准 |
|---|---|---|
| Q1 | Owner 可读性 | 无内部术语堆砌；结论/操作与说明分离；示例记录一看即懂 |
| Q2 | 取证深度 | 引用的现行规则不仅正确，而且被真正用于设计决策（而非装饰） |
| Q3 | 设计的可试跑性 | 提示词片段与测试设计可原样执行，无需评分者补全 |

## C. 试跑测试（决定性结果项；两臂各一次，由 Fable 用全新隔离子代理执行）

固定场景 T1：给一个只加载了该臂"第 4 节提示词片段"的全新 agent 提供信息——"用户常用 Ubuntu 22.04、zsh、Python 3.12 + venv、偏好 rg 而非 grep、编辑器 VSCode"——要求其按片段记录；第二轮不再提供信息，问"该用什么搜索工具、Python 怎么建环境"，并在第三轮告知"rg 偏好已过期，改用 grep"观察更新/失效处理。

| 判定 | 标准 |
|---|---|
| T1-记录 PASS | 第一轮产出符合第 2 节格式的记录（字段与位置一致） |
| T1-加载 PASS | 第二轮答案与记录一致且声明依据来自记录 |
| T1-失效 PASS | 第三轮按第 2/3 节的失效规则处理（标记/更新），不静默覆盖也不拒改 |

## D. 预设判定规则（冻结）

- 两臂 PASS 数之差 ≤1、分级总分之差 ≤2、且 T1 三项结果相同 → 判"本次观察无明显表面差异"，重新设计各阶段按交互便利选表面；
- 否则 → 差异方向即为设计工作的表面偏好，但**只作本次观察**，并记录差异集中在哪类项（客观/分级/试跑），以判断差异来自"取证能力"还是"表达习惯"；
- Pro 盲评与 Fable 评分分歧 ≥2 项时，分歧项以 Owner 裁定为准，并入登记簿。

## E. 混杂因素（预先声明）

- 网页臂读取靠 Project 知识快照，Code 臂靠本地文件——取证能力差异是被测对象的一部分，不是混杂；
- 两臂效力/思考档位由各表面设置决定，无法完全对齐——记录实际设置；
- 评分者 Fable 与 B 臂同表面同族——由 Pro 盲评对冲。
