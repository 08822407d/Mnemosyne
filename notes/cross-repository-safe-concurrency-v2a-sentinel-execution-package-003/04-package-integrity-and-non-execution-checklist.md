# V2-A A0 Package 003 — Integrity and Non-Execution Checklist

```yaml
artifact_id: MNE-CROSS-REPOSITORY-SAFE-CONCURRENCY-V2A-SENTINEL-INTEGRITY-003
package_id: MNE-CROSS-REPOSITORY-SAFE-CONCURRENCY-V2A-SENTINEL-PACKAGE-003
status: preparation_checklist_not_execution
```

## 1. Required files

```text
README.md
00-delta-precedence-and-provenance-contract.md
01-package-and-source-manifest.md
02-next-tier-controller-amendment.md
03-startup-message.md
04-package-integrity-and-non-execution-checklist.md
```

All six files must exist before package 003 is complete.

## 2. Semantic checks

Confirm:

- package 002 core source-binding repair is accepted rather than duplicated or silently replaced;
- package 003 supersedes only model-selection authorization/provenance scope;
- Owner G2A and controller startup are the same exact authority-bearing message;
- candidate-003 and manifest-003 blobs are bound;
- authorized and actual selected visible labels both occur verbatim in the message;
- exact string comparison is required;
- missing/mismatched selection blocks before branch creation;
- UI label does not attest backend identity;
- parent validation repository, fixture, historical refs, branch and seven-file write boundaries are unchanged;
- no eighth output file is authorized;
- no hidden prior conversation is required by the controller;
- the known parallel Mnemosyne write route is treated as a scheduling gate, not silently ignored.

## 3. Provenance correction checks

Confirm:

- Owner report of prior next-tier selection is recorded as direct user evidence;
- exact prior UI label remains unknown rather than guessed;
- PR #292's `operator_selection_verbatim: Pro` is superseded only for attribution scope;
- current MNEMOSYNE-226 Pro review is a separate event;
- historical PR #292 and MNEMOSYNE-224 files are not edited;
- no hidden backend identity is claimed.

## 4. Non-execution checklist

All must remain false:

```yaml
G2A_issued: false
validation_controller_branch_created: false
validation_repository_written: false
A0_executed: false
A1_to_A7_executed: false
V2_B_or_V2_C_executed: false
connector_or_app_changed: false
external_quota_consumed: false
web_Deep_Research_or_Fable_started: false
private_or_real_target_material_used: false
Meta_Agent_modified: false
real_target_modified: false
execution_source_modified: false
automatic_retry_or_compensation: false
reset_or_force_push: false
```

## 5. Parallel-route checks

Before branch creation and again before PR creation:

- read latest Mnemosyne `master`;
- enumerate all open PRs;
- list known active branches;
- inspect changed paths for `mnemosyne-225-f1-bounded-validation-design-and-next-step-write-visibility` or its successor;
- confirm no path overlap;
- record that only one PR will be open after publication;
- state that G2A waits for any route expected to move protected `master` during A0.

## 6. Future G2A preflight

After package 003 merges, fresh Pro must re-read:

- candidate-003 blob;
- manifest-003 blob;
- all package-003 and inherited package-002 blobs;
- load-bearing source blobs;
- current Mnemosyne/Meta-Agent masters;
- validation master/fixture/V1 inventory;
- controller branch and related PR absence;
- actual available visible model label;
- known write routes expected during A0.

Only then may it provide the fully populated package-003 startup/G2A message. No repository publication follows that G2A before A0.

## 7. No broader policy adoption

This package does not amend the execution source or a global model-state guard. The model-selection incident and open questions are preserved for the Owner's later dedicated Mnemosyne construction conversation.
