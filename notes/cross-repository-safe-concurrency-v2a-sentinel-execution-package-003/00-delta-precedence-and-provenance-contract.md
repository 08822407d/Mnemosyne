# V2-A A0 Package 003 — Delta, Precedence and Provenance Contract

```yaml
artifact_id: MNE-CROSS-REPOSITORY-SAFE-CONCURRENCY-V2A-SENTINEL-DELTA-CONTRACT-003
package_id: MNE-CROSS-REPOSITORY-SAFE-CONCURRENCY-V2A-SENTINEL-PACKAGE-003
status: frozen_delta_not_authorization
```

## 1. Parent disposition

Fresh Pro review accepts package 002's technical source-binding repair but rejects its readiness for G2A until the exact authorized visible model label is carried into the controller's instruction.

```yaml
parent_package_002:
  source_binding_repair: accepted
  validation_topology: accepted
  repository_and_write_boundaries: accepted
  no_retry_and_stop_rules: accepted
  run_context_claim_that_Pro_produced_MNEMOSYNE_224: rejected
  startup_model_authorization_binding: materially_incomplete
```

## 2. Exact superseded scope

Package 003 supersedes only:

- package 002 startup language claiming four dynamic values;
- any controller assumption that the selected UI label is visible without an exact Owner instruction;
- any reliance on MNEMOSYNE-224's incorrect `operator_selection_verbatim: Pro` record;
- candidate 002 as the controlling future G2A decision identity.

All other package-002 rules remain controlling and are incorporated by exact reference.

## 3. Model-selection evidence contract

The following claims must remain distinct:

```yaml
Owner_authorized_label:
  evidence_class: direct_user_instruction
  source: exact_G2A_startup_message

operator_actual_selection:
  evidence_class: operator_observed_or_operator_reported
  source: current_run_receipt

backend_identity:
  status: unknown_or_not_attestable
```

Rules:

1. The exact authorized label must occur verbatim in the message received by the controller.
2. The controller must quote the exact authorized label and the exact observed/reported selected label.
3. Equality is checked as an exact string comparison after preserving both raw values.
4. If the UI uses a different label, the option is absent, or the actual selection cannot be established, return `BLOCKED` before branch creation.
5. No response style, latency, prior-turn state or model self-report can fill a missing receipt.
6. Matching labels do not attest the hidden backend.

## 4. Owner G2A and startup are one authority-bearing message

The message sent to the fresh controller is itself the task-local Owner G2A authorization. There is no hidden prior authorization that the controller is expected to infer from another conversation.

The message must bind:

- candidate 003 blob;
- manifest 003 blob;
- execution-window protected Mnemosyne master;
- execution-window protected Meta-Agent master;
- exact authorized visible model label;
- A0-only scope;
- validation repository, branch, fixture and seven-path write boundary;
- no-retry and prohibited actions.

The controller preserves that message verbatim or by an exact stable reference in its result bundle.

## 5. Current-run scheduling condition

Before the Owner issues G2A, fresh Pro must enumerate:

- current Mnemosyne `master`;
- open PRs;
- active known write branches or conversations expected to publish;
- current Meta-Agent `master`;
- validation repository hard dependencies and controller-branch absence.

If another route is expected to move a protected ref during A0, do not issue G2A until it is completed, abandoned or explicitly paused.

This scheduling gate does not convert every unmerged branch into a global lock. It applies when a known route is expected to publish during the short A0 execution window.

## 6. Historical correction boundary

The following remain historical artifacts and are not edited:

- PR #292 body;
- MNEMOSYNE-224 result, verification and finalization;
- package 001 and package 002;
- previous assistant response.

Their technical claims are interpreted through the fresh Pro review; their Pro operator-selection claim is superseded for attribution scope by the incident record.
