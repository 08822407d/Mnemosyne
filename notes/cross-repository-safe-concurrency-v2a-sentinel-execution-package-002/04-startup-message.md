# V2-A A0 Sentinel — Future Startup Message v2

> Use only after package 002 is merged, a fresh Pro gate supplies exact execution-window refs and merged blob identities, and the Owner explicitly issues G2A authorization.

## 1. Future operator setup

Open one fresh ChatGPT conversation with the GitHub connector.

Select the **exact visible model option named in the Owner G2A authorization** and record model/reasoning labels verbatim. Current recommendation remains `gpt-5.6 sol extra high` if it is still available. If the authorized option is absent, stop and return to Pro; do not substitute.

Do not enable web search, Deep Research, Fable, another connector/app or private-file access.

## 2. Single startup message template

The Owner authorization will supply four exact values:

```text
<DECISION_CANDIDATE_002_BLOB>
<SOURCE_MANIFEST_002_BLOB>
<PROTECTED_MNEMOSYNE_MASTER>
<PROTECTED_META_AGENT_MASTER>
```

Send one substantive startup message using those exact values:

```text
@GitHub 请严格执行已合并的
MNE-CROSS-REPOSITORY-SAFE-CONCURRENCY-V2A-SENTINEL-PACKAGE-002，
仅运行 A0 sentinel。

本次 Owner G2A authorization 绑定：
- RUN-DECISION-CANDIDATE-002 blob = <DECISION_CANDIDATE_002_BLOB>
- source-manifest-002 blob = <SOURCE_MANIFEST_002_BLOB>
- execution-window protected Mnemosyne master = <PROTECTED_MNEMOSYNE_MASTER>
- execution-window protected Meta-Agent master = <PROTECTED_META_AGENT_MASTER>

先执行完全只读 preflight：验证 candidate/manifest blobs、manifest 中全部 load-bearing source blobs、validation master、fixture tree、完整 tlr-v1-* inventory、controller branch absence、模型/工具/材料边界，以及上述两个 protected master refs。

注意：不要要求 Mnemosyne master 等于 package 001/002 发布前的历史 parent SHA。source integrity 由 exact blobs 证明；上述 protected master refs 才是本次运行窗口的 no-write baseline。

任一必需条件不一致或未知时，返回 BLOCKED 并停止，不得创建分支、刷新预期值、repair package 或重试。

只有 receive PASS 后，才允许在
08822407d/mnemosyne-target-lifecycle-validation-002
从 master@e8e3296922185b4b70997c2351d6f39423f2cd4f
创建 v2a-sentinel-001-controller。

仅读取 tlr-v1-fixture-base@81f18eb5dcc6a6e68e496f67ae8f8eae782226e6；
不得修改 fixture、master 或任何 tlr-v1-* ref。
只写 package 002 中冻结的七个 A0 结果路径。

不要创建 worker/fixture branch 或 PR；不要运行 A1–A7、V2-B 或 V2-C；不要写 Mnemosyne、Meta-Agent 或任何真实目标；不要使用 Web、Deep Research、Fable、其他 app、私有材料或 external quota；失败后不要重试。

完成七文件 bundle 和 protected-ref after checks 后立即停止，把 controller branch final head、每个输出 blob/commit、可见模型/推理标签、incident ledger、before/after refs 与全部限制返回给一段全新的 Pro 对话裁决。
```

Do not send a preliminary visibility probe. Package 002 defines the only task.

## 3. Operator-visible stop conditions

Stop and return to Pro if the controller reports:

- candidate or source-manifest blob mismatch;
- any load-bearing source blob mismatch;
- protected Mnemosyne/Meta-Agent execution-window ref mismatch;
- validation master/fixture/V1 inventory mismatch;
- existing controller branch or competing PR;
- model/surface mismatch;
- uncertain material classification;
- inability to enforce exact branch/path scope;
- need to edit/repair the package;
- request for another app/private target;
- any attempt to start A1–A7 or create worker/PR.

Do not authorize a retry in the same conversation.

## 4. Return to fresh Pro

Return:

- all seven output files;
- controller branch and final head;
- output path/blob/commit identities;
- protected before/after refs;
- visible product/model/reasoning labels;
- provider/connector warnings;
- incidents and unresolved gaps.

Fresh Pro decides A0 validity and whether full V2-A preparation is justified.
