# 阶段2 分诊 — 优先级矩阵与逐束修复建议

```yaml
track_id: FABLE5-REVIEW2-001
record_type: triage_priority_matrix_and_repair_proposals
generated_by_model: claude-fable-5
surface: vscode
date: 2026-08-22
base_master_sha: 72b225d6a2faf42639cdc61c8b536439ccfdddce
evidence_class: DESIGN_RECOMMENDATION_over_phase1_labeled_findings
authority_level: non_execution_source_advisory_evidence
effort_scale:
  S: 单次会话内可完成的小修/小文档
  M: 一个完整授权任务（含 result record 与 PR）
  L: 一份完整设计稿或一轮多步工作
priority_scale:
  P1: 建议立即排期（本周~下周）
  P2: 建议一个月内完成
  P3: 有闲再做，或建议直接显式销账
```

## 束1 — 执行源时效性修订（P1）

**覆盖发现**：R2-CONF-001（§7 死条款）、R2-CONF-002（§5 快照过期）、R2-CONF-003（§10 Codex 事实过期）、R2-CONF-004（§14 平台前提过期）、专题08 的 GF5-TRIAGE-008 升级与 refresh-gate 失效。

**修复建议**（一次 spec 修订任务打包四处）：

| 条款 | 建议改法 | 替代方案 |
|---|---|---|
| §7 | 改为："新会话入口 = notes/ai-onboarding/ 包 + 当前任务的工作令/handoff；active-context 与 handoff-current 不再是必读入口"；同步决定这三个冻结文件的去向（归档声明 或 恢复为薄指针） | 恢复维护 active-context（不推荐：重复造总路标，且历史证明维护不可持续） |
| §5 | 删除"7 份报告"快照句，改为指向 research-report-index 作为当前证据层权威视图 | 每次新 cycle 都改 spec（不推荐：churn） |
| §10 | 删除/改写 Codex Cloud 角色句为工具无关表述（"远程与本地写入助手按当前平台事实文件适用"） | 保留但标 stale（不推荐：执行源内容不应长期带 stale 标） |
| §14 | 保留安全内核，把"Codex Cloud 附件限制"从规则前提降为历史动机注记 | 触发 §14 自带的修订钩子做平台复核后再改（成本高收益低） |

**代价**：案文起草 L 的一小部分（阶段3 候选 B，Fable 免费额度内）；Owner 审批 ~20–30 分钟；执行 M（1 个任务，§6 流程：candidate → Owner 确认 → 修订）。
**不修的后果**：每个照章程冷启动的会话都从错误路标出发；§4 冲突登记义务持续违反中。
**依赖**：无。可独立先行。

## 束2 — 规则层治理设计（P1，阶段3 候选 A）

**覆盖发现**：R2-CONF-005（guard 层无地位）、R2-COST-001（加载负担确诊未治）、R2-COST-006（无整合机制）、MNEMOSYNE-199 方案搁浅、R2-CONF-007（语言分层，并入）。

**设计稿应回答的四个问题**（这正是需要独立设计而非直接修的原因——四问互相耦合）：

1. **地位**：guard 层在权威体系中是什么？（spec 内定义"行为规范层"+清单指针 / 少数吸收入 spec 其余降级 / spec 因式分解——第一轮 UD 遗留的正式重开）
2. **加载**：谁必读、谁触发读？（199 的 3+9 方案是现成起点，需决定承载文件与迁移步骤）
3. **整编**：什么机制防止只加不减？（建议：每 N 份新 guard 或每月触发一次整编评审，产出合并/退役提案）
4. **语言**：人读层与模型层的语言规则（中文摘要义务的范围与格式）

**代价**：设计稿 L（Fable，本轨道内）；Owner 批稿 ~30 分钟；实施 M×2（loader/guard 改造 + spec 侧确认）。
**不修的后果**：规则文本按事故率单调增长；加载税已 3800 行且无上限；两族模型对规则的解释分歧风险随文本量上升。
**依赖**：与束1 有一处交叉（若选 spec 因式分解则束1 案文并入本设计），建议同批做。

## 束3 — 状态失效纪律（P2，机制部分并入束2）

**覆盖发现**：R2-FRESH-001/002/003/006、GF5-ENH-CUR-004 升级、R2-SCALE-006。

**修复建议**分两半：
- **即时修复**（S，1 个任务）：按束1 定下的 §7 去向处理三个冻结文件；修 review-and-validation-status 的 greenfield 过期段；决定总路标去留（建议：不恢复总路标，改为 onboarding 指明"以 per-route status 为准 + 枚举方法"，少一个要维护的 live 文件）。
- **机制**（并入束2 设计稿）：所有自称 live/current 的文件强制 `last_updated_by_task` + 失效声明（"下一个改变本领域状态的任务必须更新本文件"），写入方 checklist 承载，不引自动化。

**代价**：S+设计并入；Owner ~10 分钟。
**不修的后果**：过期路标继续误导；A/B 立项后 status 文件数量放大问题（R2-SCALE-006）。
**依赖**：即时修复依赖束1 的 §7 决定；先后关系明确。

## 束4 — Owner 决策打包（P1，纯决策零执行）

内容即 `02-owner-decision-package.md`，8 问 30–60 分钟。此处只列风险排序理由：

- D-01（F2 G2A 门）与 D-02（#265 结算 + A/B 立项）是仅有的两项 HIGH 债，且互相耦合（R2-SCALE-002 的先后依赖选择题包含其中）。
- 其余 6 问多数是"一句话销账/追认"级别。

**不答的后果**：决策债利息最高——语境冷却后重建成本远超现在回答的成本。

## 束5 — 历史收尾（P3）

| 项 | 建议 | 代价 |
|---|---|---|
| D-03 机械 no-write 门 BLOCKED + D-05 W4 | 显式作废（对象已毕业迁出；保留历史挂账记录，加一行"对象退役、门作废"的收尾注记） | S，搭任意维护任务的车 |
| D-06 16 个外部证据分支 + D-07 capsule 分支 | 做一次证据保全设计（阶段3 候选 D）：外部分支内容的不可变归档方案 + 归档后的清理放行流程 | 设计 M；执行 M |
| GF5-TRIAGE-001（FR-01/03 研究） | 销账或窄化重定范围（专题08 已给二选一），推荐销账——原题的研究对象已被现实事件与新事实文件覆盖 | Owner 一句话 |
| GF5-ENH-CUR-003（no-re-ask） | 销账（目标被组合机制覆盖） | Owner 一句话 |

## 束6 — 多写入方常态化（P2，搭既定日程）

**覆盖**：D-17 署名方案（已排期联合确认）、R2-CONF-006（§18 泛化为任意 Agent 写入面）、R2-SCALE-004（任务号命名空间追认）、R2-CORE-004/R2-SPOF-002（跨族最小惯例）。

**建议**：联合确认会的议程从"只确认署名方案"扩为四合一（署名 + §18 泛化方向 + 命名空间追认 + 跨族最小惯例清单），一次会解决整束；会后 1 个跟进任务落地。
**代价**：Owner 在既定会议上 +10 分钟；跟进 M。
**依赖**：无；但束6 完成前 Claude 产出继续隔离文件夹（现状可接受）。

## 单列项

| 项 | 优先级 | 建议 | 代价 |
|---|---|---|---|
| 交接真实效果评估（TODO 4 / R2-SPOF-006） | P2 | 阶段3 候选 E：把 Owner 的方法论落成可执行评估设计（样本主线清单、输入隔离规则、评分口径、执行面选择）；执行需对话档案访问，方式由设计稿给选项 | 设计 L；执行另计（可能涉及付费模型调用，设计稿会给成本档位） |
| 真实需求 A/B 立项 | 由 D-02 决定 | 若立项：阶段3 候选 F 产出 intake 包（含 B 的敏感数据外置预案——公开仓库约束下 Owner 个人学习数据默认不入库）；流程用分级版（R2-SCALE-001 的 light 通道设计并入） | 设计 L；立项后走 §16 |
| Fable 异构复核本轮结果 | P2（可选） | 下周 Pro 恢复后，把本轨道全部产出交 ChatGPT Pro 做一次异构裁定（对冲 §19 同族限制）——重演第一轮"Fable 评审 → Pro 裁定"的成功模式 | Pro 一次会话；Owner 转交 ~10 分钟 |

## 全量发现 → 处置映射校验

阶段1 的 9 条 REPAIR、8 条 NON_BLOCKING、3 条 QUESTION 与 17 项债务已全部映射入上述束/单列项，无遗漏（NON_BLOCKING 与 OBSERVATION 级中不需动作者未列入矩阵，它们的价值已沉淀在阶段1 报告中）。映射完整性可用阶段1 总摘要的索引表逐条核对。
